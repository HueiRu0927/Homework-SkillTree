@model IList<Homework_SkillTree.ViewModel.CategoryInputViewModel>
@using Homework_SkillTree.Models;

@*@{
    List<CategoryInputViewModel> categoryListViewModel = (List<CategoryInputViewModel>)ViewBag.Category;
}*@

<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">

            <tr>
                <th>#</th>
                <th>@Html.DisplayNameFor(x=>x.FirstOrDefault().Categories)</th>
                <th>@Html.DisplayNameFor(x=>x.FirstOrDefault().Date)</th>
                <th>@Html.DisplayNameFor(x=>x.FirstOrDefault().Money)</th>
            </tr>
            @{
                var i = 1;
                foreach (var item in Model)
                {
                    <tr>
                        <td>@(i++)</td>
                        <td class="@item.Categories">@Html.DisplayFor(x=>item.Categories)</td>
                        <td class="@item.Date">@Html.DisplayFor(x=>item.Date)</td>
                        <td class="@item.Money">@Html.DisplayFor(x=>item.Money)</td>
                    </tr>
                }
            }
            
        </table>
    </div>
</div>
